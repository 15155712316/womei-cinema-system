# 基础依赖包
requests>=2.25.0
ttkbootstrap>=1.10.0
PyQt5>=5.15.0
Pillow>=8.0.0

# GUI和Web相关
PyQtWebEngine>=5.15.0
qrcode[pil]>=7.4.2

# 打包和加密
PyInstaller>=5.13.2
cryptography>=3.0.0

# 工具包
setuptools
wheel

# 网络相关
urllib3
certifi
charset-normalizer
idna

# 注意：pywin32>=227 仅适用于Windows系统，macOS不需要此依赖